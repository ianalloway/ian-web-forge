# This site has no required environment variables.
#
# The newsletter and contact forms are handled entirely by Netlify Forms
# (see index.html + netlify.toml) — no backend, no API keys, nothing to
# configure here. This file is a placeholder in case future features need
# build-time env vars.
